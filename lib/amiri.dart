import 'package:flutter/material.dart';

class AmiriPage extends StatelessWidget {
  const AmiriPage ({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Amiri"),
      ),
    );
  }
}