import 'package:flutter/material.dart';

class GucciPage extends StatelessWidget {
  const GucciPage ({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Gucci"),
      ),
      body: Text(""),
    );
  }
}