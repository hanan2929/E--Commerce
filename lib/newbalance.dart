import 'package:flutter/material.dart';

class NewbalancePage extends StatelessWidget {
  const NewbalancePage ({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Newbalance"),
      ),
    );
  }
}